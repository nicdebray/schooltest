<%= simple_form_for @student do |f| %>
<%= f.input :first_name, label: "Student's first name" %>
<%= f.input :last_name, label: "Student's last name" %>
<%= f.button :submit, class: "button-green button" %>
<% end %>
