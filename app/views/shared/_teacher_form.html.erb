<%= simple_form_for @teacher do |f| %>
<%= f.input :first_name, label: "Teacher's first name" %>
<%= f.input :last_name, label: "Teacher's last name" %>
<%= f.button :submit, class: "btn btn-success" %>
<% end %>
