<div class="container-fluid below-navbar">
  <h1>Edit <%= @course.name %> course</h1>
  <p>You can change and update <%= @course.name %> course settings</p>
  <div class="new-form-create col-xs-12 col-sm-6">
  <%= render 'shared/course_form', course: @course %>
  </div>
</div>
